<!DOCTYPE html>
<html lang="da">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title><%= typeof title !== 'undefined' ? title : 'Fejl' %></title>
  <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-slate-50 min-h-screen flex items-center justify-center p-4">
  <div class="bg-white rounded-xl shadow-lg p-8 max-w-md w-full text-center">
    <h1 class="text-2xl font-semibold text-slate-800 mb-4">
      <%= typeof title !== 'undefined' ? title : 'Der opstod en fejl' %>
    </h1>
    <p class="text-slate-600 mb-6">
      <%= typeof message !== 'undefined' ? message : 'Prøv igen senere.' %>
    </p>
    <a href="javascript:history.back()" class="inline-block px-4 py-2 bg-slate-800 text-white rounded-lg hover:bg-slate-700">
      Gå tilbage
    </a>
  </div>
</body>
</html>
