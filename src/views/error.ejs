<!DOCTYPE html>
<html lang="da">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1, maximum-scale=1">
  <title><%= typeof title !== 'undefined' ? title : 'Fejl' %></title>
  <script src="https://cdn.tailwindcss.com"></script>
  <style>
    html, body { overflow-x: hidden; }
  </style>
</head>
<body class="bg-slate-50 min-h-screen flex items-center justify-center p-4 text-slate-800 antialiased">
  <div class="bg-white rounded-xl shadow-sm p-6 sm:p-8 max-w-[480px] w-full text-center">
    <h1 class="text-xl sm:text-2xl font-semibold text-slate-800 mb-4">
      <%= typeof title !== 'undefined' ? title : 'Der opstod en fejl' %>
    </h1>
    <p class="text-base text-slate-600 mb-6 leading-relaxed">
      <%= typeof message !== 'undefined' ? message : 'Prøv igen senere.' %>
    </p>
    <a href="javascript:history.back()" class="inline-flex items-center justify-center min-h-[48px] px-6 py-3 bg-slate-800 text-white text-base font-medium rounded-xl hover:bg-slate-700 active:bg-slate-600">
      Gå tilbage
    </a>
  </div>
</body>
</html>
